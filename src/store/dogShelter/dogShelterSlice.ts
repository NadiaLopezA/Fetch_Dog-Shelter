import { createSlice } from '@reduxjs/toolkit';

export const dogShelterSlice = createSlice({
    name: 'auth',
    initialState: {

    },
    reducers: {

    }
});

export const { } = dogShelterSlice.actions;